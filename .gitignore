# Secrets. Never commit.
.env

# Generated by the lab
out/
!out/.gitkeep

# Embedding cache — real vectors, real money. Keep them, don't commit them.
.cache/

__pycache__/
*.pyc
.DS_Store

# This lab needs no virtualenv — it is stdlib only. Ignored anyway, in case a
# learner makes one out of habit.
.venv/
venv/
